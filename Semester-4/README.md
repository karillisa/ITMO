# Gradle Deamon
**Gradle Daemon** — это **фоновый процесс**, который ускоряет выполнение задач в Gradle, оставаясь запущенным между сборками. Он позволяет избежать повторной инициализации JVM и загрузки проекта при каждом запуске команды.

---

## 🧱 Что такое Gradle Daemon?

### Основное определение:
> **Daemon (демон)** — это фоновый процесс, который остаётся активным после первого запуска Gradle и переиспользуется для последующих сборок.

### Зачем он нужен?
- Чтобы **не создавать каждый раз новую JVM**.
- Чтобы **не перечитывать проект и плагины заново**.
- Чтобы **ускорить выполнение задач**, особенно для больших проектов.

---

## ✅ Преимущества Gradle Daemon

| Плюс | Описание |
|------|----------|
| 🔥 Быстрее сборка | Не нужно загружать классы, парсить `build.gradle`, инициализировать JVM снова. |
| 💡 Кэширование | Daemon кэширует состояние проекта, что ускоряет последующие сборки. |
| 🧠 Поддерживает инкрементальные сборки | Использует информацию о предыдущих состояниях файлов. |
| 🚀 Удобство при разработке | Особенно полезен при частых изменениях кода и тестировании. |

---

## ❌ Недостатки Gradle Daemon

| Минус | Описание |
|-------|----------|
| 🧯 Использование памяти | Daemon держит JVM в памяти даже если вы не работаете с проектом. |
| 🧊 Иногда "зависает" | Может использовать старое состояние проекта, особенно при обновлении зависимостей. |
| 🧹 Требует ручного управления | Иногда его нужно перезапускать или останавливать вручную. |

---

## 📦 Как Daemon работает под капотом

1. **Первый запуск:**
   - Gradle создаёт новый процесс (JVM).
   - Загружает проект (`settings.gradle`, `build.gradle`).
   - Выполняет задачи.

2. **Последующие запуски:**
   - Если Daemon всё ещё активен, Gradle использует его.
   - Проект не загружается заново, используются уже загруженные классы и данные.

3. **Daemon "умирает":**
   - Через некоторое время бездействия (по умолчанию 3 часа).
   - При явном вызове `--stop`.
   - При сбое или завершении сессии.

---

## 🛠️ Как управлять Daemon

### 1. **Запуск с Daemon (по умолчанию)**
```bash
./gradlew build
```
> Daemon автоматически включён по умолчанию.

---

### 2. **Отключение Daemon**
Если хочешь отключить Daemon (например, для проверки "чистой" сборки):
```bash
./gradlew build --no-daemon
```

---

### 3. **Остановка всех активных демонов**
```bash
./gradlew --stop
```
> Полезно, если ты видишь странные ошибки или хочешь очистить кэш.

---

### 4. **Настройка времени жизни Daemon**
В файле `gradle.properties` можно указать:
```properties
org.gradle.daemon.idletimeout=10800000
```
> Время бездействия в миллисекундах (по умолчанию 3 часа = 10 800 000 мс).

---

### 5. **Изменение максимального размера памяти Daemon**
Тоже в `gradle.properties`:
```properties
org.gradle.jvmargs=-Xms512m -Xmx2048m
```
> Увеличь, если у тебя большой проект или много плагинов.

---

## 🕵️ Как узнать, работает ли Daemon?

Выполни:
```bash
./gradlew help --system-properties
```
Или просто:
```bash
jps -l
```
> В выводе должен быть процесс `org.gradle.launcher.daemon.bootstrap.GradleDaemon`.

---

## 🧪 Когда стоит отключать Daemon?

- При CI/CD (напр., GitHub Actions, Jenkins): там он бесполезен, т.к. окружение одноразовое.
- При проблемах с кэшированием (например, зависимости не обновляются).
- На слабых машинах, где память ограничена.

---

## 🧰 Где хранится информация о Daemon?

- **Windows:**  
  ```
  C:\Users\<your_user>\.gradle\daemon\
  ```

- **Linux/macOS:**  
  ```
  ~/.gradle/daemon/
  ```

---

## 📈 Пример влияния Daemon на скорость

| Сборка         | Время (без Daemon) | Время (с Daemon) |
|----------------|--------------------|-------------------|
| Первая         | 20 сек             | 20 сек            |
| Вторая         | 18–20 сек          | 3–5 сек           |
| Третья         | 18–20 сек          | 2–3 сек           |

---

## 📌 Резюме

| Факт | Описание |
|------|----------|
| **Daemon** | Это фоновый процесс, ускоряющий сборку Gradle. |
| **По умолчанию** | Включён автоматически. |
| **Управление** | Можно отключить через `--no-daemon` или остановить через `--stop`. |
| **Кэширование** | Ускоряет повторные сборки. |
| **Недостатки** | Использует память, иногда может давать устаревшие результаты. |

---


# Build cache 
### 🧱 Что такое **Build Cache** в Gradle?

**Build Cache (кэш сборки)** — это механизм, который позволяет **сохранять результаты выполнения задач между сборками**, чтобы избежать их повторного выполнения. Это особенно полезно при разработке, CI/CD и работе с несколькими окружениями.

---

## ✅ Основная цель Build Cache:

> **Не выполнять одни и те же задачи заново**, если входные данные не изменились.

Например:
- Если вы уже компилировали Java-классы и исходники не менялись → Gradle загрузит готовые `.class` из кэша.
- То же самое с тестами, JAR/WAR, Lint, проверкой кода и т.д.

---

## 🚀 Как работает Build Cache

1. **Gradle определяет входы и выходы задачи**:
   - Входы: файлы, параметры, зависимости.
   - Выходы: результат выполнения задачи (например, `build/classes/java/main`).

2. **Вычисляется хэш задачи на основе входов**.

3. **Если такой хэш уже есть в кэше**:
   - Результат загружается из кэша.
   - Задача помечается как `FROM-CACHE`.

4. **Если изменений нет**:
   - Никакие задачи не выполняются заново.
   - Сборка становится **очень быстрой**.

---

## 📁 Типы Build Cache в Gradle

| Тип | Описание |
|-----|----------|
| **Local Build Cache** | Хранится локально на машине. По умолчанию включён. |
| **Remote Build Cache** | Общий кэш для нескольких машин (CI/CD, команда). Реализуется через сервер (например, Artifactory или собственный). |

---

## 🔧 Как включить Local Build Cache

Добавь в `gradle.properties`:

```properties
org.gradle.caching=true
```

Теперь Gradle будет сохранять результаты задач в:

- **Windows:**  
  ```
  C:\Users\<user>\.gradle\caches\
  ```

- **Linux/macOS:**  
  ```
  ~/.gradle/caches/
  ```

---

## 🌐 Remote Build Cache (распределённый)

Для командной работы или CI/CD можно использовать **удалённый кэш**:

Пример настройки в `settings.gradle(.kts)`:

### `settings.gradle.kts`
```kotlin
buildCache {
    remote<HttpBuildCache> {
        url = uri("https://your-artifactory-server/artifactory/gradle-build-cache")
        isAllowUntrustedServer = true
        credentials {
            username = "user"
            password = "password"
        }
    }
}
```

### `settings.gradle` (Groovy)
```groovy
buildCache {
    remote(HttpBuildCache) {
        url = 'https://your-artifactory-server/artifactory/gradle-build-cache'
        allowUntrustedServer = true
        credentials {
            username = 'user'
            password = 'password'
        }
    }
}
```

---

## 🧪 Когда помогает Build Cache?

| Сценарий | Помогает? | Почему? |
|---------|-----------|----------|
| Локальная разработка | ✅ | Быстро пересобираешь после небольших правок. |
| CI/CD | ✅✅ | Ускоряет pipeline, особенно если используется удалённый кэш. |
| Многомодульные проекты | ✅✅✅ | Модули, которые не изменились, берутся из кэша. |
| Частые clean сборки | ✅ | Кэшированные результаты остаются доступны. |

---

## 🛑 Когда Build Cache может не помочь?

| Причина | Объяснение |
|--------|------------|
| Изменяющиеся входы | Например, timestamp в ресурсах → Gradle не сможет кэшировать. |
| Random UUIDs или динамические версии (`latest.release`) | Входы меняются каждый раз. |
| Некорректно написанные плагины | Не все задачи могут быть кэшируемыми. |
| Использование `--no-build-cache` | Явное отключение кэша. |

---

## 📊 Как посмотреть, какие задачи взяты из кэша?

Запускай с флагом:

```bash
./gradlew build --info
```

Или используй:

```bash
./gradlew build -Dorg.gradle.logging.level=INFO
```

В выводе будут строки вроде:

```
Task :compileJava UP-TO-DATE
Task :test FROM-CACHE
```

---

## 🧹 Как очистить кэш?

### 1. **Локальный кэш**
Удалите папку:
- **Windows:**  
  ```
  C:\Users\<user>\.gradle\caches\
  ```

- **Linux/macOS:**  
  ```
  ~/.gradle/caches/
  ```

### 2. **Очистка через команду**
```bash
./gradlew --stop
rm -rf ~/.gradle/caches/
```

---

## ⚙️ Полезные настройки в `gradle.properties`

```properties
# Включить кэширование
org.gradle.caching=true

# Настройка максимального размера кэша (по умолчанию 5 ГБ)
org.gradle.cache.maxsize=8192

# Отключить кэширование (временно)
# org.gradle.caching=false
```

---

## 📌 Итог

| Функция | Команда / настройка |
|--------|----------------------|
| Включить кэш | `org.gradle.caching=true` |
| Локальный кэш | Сохраняется в `~/.gradle/caches/` |
| Удалённый кэш | Через `remote(HttpBuildCache)` |
| Посмотреть, что из кэша | `--info` |
| Очистить кэш | Удалить папку кэша |
| Отключить кэш временно | `--no-build-cache` |

---

#  build vs assemble 
В Gradle термины **`build`** и **`assemble`** — это **стандартные задачи (tasks)**, но они выполняют разные цели. Вот подробное сравнение:

---

## 📌 1. `assemble`

### Что это:
- Это **часть процесса сборки**, которая отвечает за **упаковку проекта** в финальный артефакт: JAR, WAR, AAR, APK и т.д.

### Что делает:
- Собирает проект: объединяет скомпилированные классы, ресурсы, зависимости.
- Создаёт финальный артефакт: например, `app.jar`, `project.war`.

### Примеры:
```bash
./gradlew assemble
```
Создаст:
- `build/libs/project-1.0.jar` (для Java)
- `build/libs/project-1.0.war` (для web)

> Не запускает тесты!

---

## 📌 2. `build`

### Что это:
- Это **мета-задача**, которая включает в себя **несколько других задач**, в том числе `assemble`.

### Что делает:
- Выполняет полный жизненный цикл сборки:
  - `compileJava` → компиляция исходников
  - `processResources` → копирование ресурсов
  - `test` → выполнение юнит-тестов
  - `check` → проверка качества кода (если есть)
  - `assemble` → сборка финального артефакта

```bash
./gradlew build
```

> ✅ Если всё прошло успешно — ты получишь готовый артефакт + уверенность, что тесты пройдены.

---

## 🧠 Основные различия: `build` vs `assemble`

| Характеристика        | `assemble`                         | `build`                                   |
|-----------------------|------------------------------------|-------------------------------------------|
| Запускает тесты?      | ❌ Нет                              | ✅ Да                                      |
| Компилирует код?      | ❌ Нет (только упаковывает)         | ✅ Да                                      |
| Упаковывает артефакт? | ✅ Да                               | ✅ Да                                      |
| Проверяет код?        | ❌ Нет                              | ✅ Может, если настроено                   |
| Используется в CI?    | ❌ Редко                            | ✅ Очень часто                              |
| Подходит для "чистой" сборки? | ✅ Только упаковка     | ✅ Полная проверка и сборка               |

---

## 🧪 Когда использовать какую команду?

| Сценарий | Рекомендуемая команда |
|----------|------------------------|
| Ты хочешь просто собрать JAR/WAR/... | `./gradlew assemble` |
| Ты хочешь убедиться, что всё работает | `./gradlew build` |
| Ты в CI/CD и хочешь протестировать перед деплоем | `./gradlew build` |
| Ты только что изменил код, но не тестировал | `./gradlew build` |
| Ты менял только ресурсы или манифест | `./gradlew assemble` |

---

## 📁 Где найти результат?

После любой из этих команд:
```
build/libs/
```

Пример:
```
build/libs/project-1.0.jar
build/libs/project-1.0.war
```

---

## 📌 Пример вывода

### `./gradlew assemble`
```
> Task :compileJava
> Task :processResources
> Task :classes
> Task :jar
> Task :war SKIPPED
> Task :assemble
```

### `./gradlew build`
```
> Task :compileJava
> Task :processResources
> Task :classes
> Task :jar
> Task :test
> Task :check
> Task :build
```

---

## 🔚 Итог

| `assemble` | `build` |
|------------|---------|
| Только собирает артефакт | Полная сборка с тестами |
| Быстрее | Дольше, но надёжнее |
| Используется при небольших правках | Используется для выпуска и CI/CD |

---

#  check (verify) 
### 🧪 Что такое `check` и `verify` в Gradle?

Эти задачи (tasks) **используются для проверки качества и корректности кода**, но с разной степенью строгости и распространённости в экосистеме Gradle.

---

## ✅ 1. `check`

### Это стандартная задача Gradle

```bash
./gradlew check
```

### Что делает:
- Запускает **все проверочные задачи**, которые есть в проекте.
- Включает:
  - `test` — запуск юнит-тестов
  - `lint`, `spotlessCheck`, `pmd`, `findbugs`, `checkstyle` — если они подключены
  - Иногда даже проверку зависимостей, миграций БД и т.д.

> Это **часть жизненного цикла сборки** (`build` зависит от `check`).

### Когда используется:
- При CI/CD перед деплоем
- Перед пушем в репозиторий
- Чтобы убедиться, что всё работает как ожидается

---

## ✅ 2. `verify` (иногда встречается)

> ⚠️ **`verify` не является стандартной задачей в Gradle по умолчанию**, но может быть добавлена вручную или через плагины.

### Где можно встретить:
- **Maven**: там `mvn verify` — это часть фазы жизненного цикла, аналогична `check`.
- **Gradle + плагины**:
  - Например, в некоторых CI-шаблонах или кастомных скриптах можно встретить задачу `verify`, которая делает то же самое, что и `check`.

### Пример ручного определения:
```groovy
task verify {
    dependsOn 'check'
}
```

Или с дополнительными проверками:
```groovy
task verify {
    dependsOn 'test', 'checkstyle', 'pmd', 'integrationTest'
}
```

---

## 🧠 Разница между `check` и `verify`

| Характеристика       | `check`                          | `verify`                             |
|----------------------|----------------------------------|---------------------------------------|
| Стандартная задача?  | ✅ Да                             | ❌ Нет (обычно кастомная)              |
| Что делает?          | Выполняет тесты и статический анализ | То же, но может быть расширена        |
| Использование        | CI, PR, local dev                | Может использоваться как альтернатива |
| Расширяемость        | Можно расширять через `dependsOn` | Полностью кастомизируется             |

---

## 📦 Какие задачи обычно включает `check`?

Если ты используешь стандартный Java/WAR проект, то:

- `test` — запуск юнит-тестов
- `checkstyleMain` / `checkstyleTest` — проверка стиля кода
- `pmdMain` / `pmdTest` — поиск потенциальных багов
- `spotlessCheck` — форматирование кода
- `errorprone` — статический анализ Java
- `jacocoTestCoverageVerification` — проверка покрытия тестами

---

## 🛠️ Как посмотреть, какие задачи выполняются при `check`

```bash
./gradlew tasks --group verification
```

Пример вывода:
```
Verification tasks
------------------
check - Runs all checks.
test - Runs the unit tests.
checkstyleMain - Performs Checkstyle analysis on main source code.
pmdMain - Performs PMD analysis on main source code.
```

---

## 🧪 Когда использовать `check`?

- После `git pull` — чтобы убедиться, что ничего не сломано
- Перед `git push` — чтобы все тесты прошли
- В CI/CD — как обязательный этап перед сборкой
- При выпуске новой версии

---

## 🧩 Пример: добавление `verify` в `build.gradle`

Если хочешь добавить свою задачу `verify`, можешь сделать так:

```groovy
task verify {
    group = 'verification'
    description = 'Runs all quality checks and tests'

    dependsOn 'check'
    dependsOn 'pmd', 'findbugs', 'spotlessCheck'
}

// Теперь можно запускать:
// ./gradlew verify
```

---

## 📌 Итог

| Команда   | Является стандартной? | Описание |
|-----------|------------------------|----------|
| `check`   | ✅ Да                   | Выполняет тесты и проверки качества |
| `verify`  | ❌ Нет (опционально)   | Может быть создан как альтернатива или надстройка над `check` |

---

# gradlew 
Команда `gradlew` — это **Gradle Wrapper**, который позволяет запускать сборку проекта без предварительной установки Gradle на компьютере. Он гарантирует, что все разработчики и CI/CD используют **одну и ту же версию Gradle**, что устраняет проблему *"у меня работает"*.

---

## 🧱 Что такое `gradlew`?

### Определение:
> `gradlew` — это **скрипт-обёртка (wrapper)** над Gradle, которая автоматически скачивает нужную версию Gradle и запускает задачи сборки.

- На Windows: используется `gradlew.bat`
- На Linux/macOS: используется `gradlew` (без расширения)

Эти файлы находятся в корне проекта:

```
project/
├── gradlew          # Linux/macOS
├── gradlew.bat      # Windows
├── gradle/wrapper/
│   ├── gradle-wrapper.jar
│   └── gradle-wrapper.properties
```

---

## ✅ Преимущества использования `gradlew`

| Плюс | Объяснение |
|------|------------|
| 📦 Нет необходимости устанавливать Gradle вручную | Скрипт сам скачает нужную версию. |
| 🔄 Гарантированная совместимость | Все участники работают с одной версией Gradle. |
| 🧪 Работает "из коробки" | Подходит для CI/CD систем, таких как GitHub Actions, Jenkins, GitLab CI. |
| 💡 Легко обновляется | Можно изменить версию Gradle через файл `gradle-wrapper.properties`. |

---

## 🛠️ Как использовать `gradlew`

Примеры команд:

```bash
# Выполнить полную сборку проекта
./gradlew build

# Только собрать артефакт (JAR/WAR)
./gradlew assemble

# Запустить тесты
./gradlew test

# Посмотреть список всех доступных задач
./gradlew tasks

# Посмотреть зависимости проекта
./gradlew dependencies

# Очистить проект
./gradlew clean

# Запустить пользовательскую задачу
./gradlew myBuild

# Остановить фоновый процесс Daemon
./gradlew --stop

# Отключить Daemon (для отладки)
./gradlew build --no-daemon

# Использовать кэш сборки
./gradlew build --build-cache
```

> ⚠️ Не забывай сделать скрипт исполняемым (на Linux/macOS):
```bash
chmod +x gradlew
```

---

## 🧩 Как работает `gradlew` внутри

1. **Проверяет**, есть ли нужная версия Gradle в локальном кэше (`~/.gradle/wrapper`).
2. Если нет — **скачивает** её из интернета.
3. **Запускает указанную задачу** (например, `build` или `test`).

---

## 🔁 Как обновить версию Gradle через `gradlew`

Открой файл:
```
gradle/wrapper/gradle-wrapper.properties
```

Измени строку:
```properties
distributionUrl=https\://services.gradle.org/distributions/gradle-8.4-bin.zip
```

Теперь выполните:
```bash
./gradlew wrapper
```

✅ Теперь `gradlew` будет использовать новую версию Gradle.

---

## 📌 Почему важно использовать `gradlew` вместо `gradle`

| Критерий | `gradle` | `gradlew` |
|---------|----------|-----------|
| Требует установки | ✅ Да     | ❌ Нет    |
| Используется в CI | ❌        | ✅        |
| Версия Gradle фиксирована | ❌       | ✅        |
| Удобство работы в команде | ❌     | ✅        |

---

## 🧪 Пример вывода при первом запуске `gradlew`

```bash
Downloading https://services.gradle.org/distributions/gradle-8.4-bin.zip
...............................................................................................
Unzipping /Users/user/.gradle/wrapper/dists/gradle-8.4-bin/abc123/gradle-8.4
```

После этого Gradle сохранится локально и будет использоваться повторно.

---

## 📌 Итог

| Вопрос | Ответ |
|-------|-------|
| Что такое `gradlew`? | Это Gradle Wrapper — скрипт для автоматической загрузки и запуска Gradle. |
| Зачем он нужен? | Чтобы все разработчики и CI/CD использовали одну версию Gradle. |
| Какие команды можно выполнять? | `build`, `assemble`, `test`, `clean`, `tasks`, `dependencies` и т.д. |
| Как обновить версию Gradle? | Изменить `distributionUrl` в `gradle-wrapper.properties`. |
| Нужно ли его коммитить в Git? | ✅ Да! Особенно `gradlew`, `gradlew.bat` и папку `gradle/wrapper`. |
